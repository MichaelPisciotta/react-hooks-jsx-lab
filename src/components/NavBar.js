import React from "react";

function NavBar() {
  // update the JSX being returned!
  return (
  <nav>
    <a href="#home">Home</a>
    <a href="#about">About</a>
  </nav>
        )}

export default NavBar;

//to link to ids within a web page, you can use # + id of element i am linking to.